import { createBrowserRouter } from 'react-router-dom';
import routes from './routes/routes';

// Create browser router with our route configuration
const router = createBrowserRouter(routes);

export default router; 